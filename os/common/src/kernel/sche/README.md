内核态（调度器 / Scheduler）:
仅负责物理 CPU 的分配和执行上下文的切换。
仅看到 “线程（Thread）”（可调度的执行流）。
执行者：根据优先级瞬间调度哪个线程上 CPU。

用户态（进程管理器 / Process Server）:
负责逻辑上的进程管理（创建、终止、命名、权限、组关系）。
管理 “进程（Process）”（地址空间、资源集、权限容器）。
策划者：决定该进程优先级应该是多少，决定谁能创建谁。